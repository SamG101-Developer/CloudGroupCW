<script>
    function createQuiz(){
        quizName = prompt("Enter the name of your quiz")
    }
</script>
<div class="container-fluid">
    <div class="row">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark navSize">
            <a class="navbar-brand">
                <img src="/static/assets/VPQ_Logo.png" width="45" height="45" alt="logo" style="padding-left: 0.2vw">
            </a>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Create</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Host</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Join</a>
                    </li>
                </ul>
            </div>
        </nav>
    </div>
    <div class="row" id="createQuizContainer">
        <button class="btn btn-lg btn-info" id="createQuizButton" @click="createQuiz()">Create</button>
        <div class="col">
            <div class="row">
                <nav class="navbar nav-tabs navbar-expand-lg" role="tablist">
                    <ul class="navbar-nav" id="createQuizTabList">
                        <li class="nav-item"><a class="nav-link" onclick="addTab()" role="tab">Add Round</a></li>
                    </ul>
                </nav>
                <div class="scroller-content" id="round-content"></div>
            </div>
        </div>
        <div class="col" id="questionSearch">
            <h2>Question Search</h2>
            <label>Enter the username you want to search for
                <input id="usernameToSearch" type="text" v-model="questionSearchUsernameField">
            </label>
            <button @click="searchDatabaseForQuestions()">Search</button>
            <div v-for="question in queriedQuestions" class="row questionSearchOption scroller-content">
                <button @click="handleAddSearchedQuestion(question)">Add Question</button>
                {{ question.questionType }}<br>
                {{ question.questionText }}<br>
                {{ question.answer }}<br>
                Options:
                <div v-for="option in question.options">
                    {{option}}
                </div>
            </div>
        </div>
    </div>
</div>
